# Roadmap

Potential upcoming features. When we decide to build one, create a project doc with a detailed task breakdown.

- Config file (`.magent/config.toml`)
- Scheduled directives (`in:`, `at:`)
- Edit arbitrary files
- Code execution / scripts (e.g. python scripts)
- Per-directive model selection
- Concurrent directive processing
